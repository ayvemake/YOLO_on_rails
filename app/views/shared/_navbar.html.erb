<nav class="bg-white shadow">
  <!-- ... autres éléments de la navbar ... -->
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex">
        <div class="flex-shrink-0 flex items-center">
          <%= link_to root_path, class: "text-blue-600 font-bold text-xl" do %>
            Pacemaker Vision
          <% end %>
        </div>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
          <%= link_to root_path, 
              class: "inline-flex items-center px-1 pt-1 border-b-2 #{current_page?(root_path) ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" do %>
            <i class="fas fa-chart-line mr-1"></i>
            Tableau de bord
          <% end %>
          
          <%= link_to analyses_path, 
              class: "inline-flex items-center px-1 pt-1 border-b-2 #{controller_name == 'analyses' ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" do %>
            <i class="fas fa-microscope mr-1"></i>
            Analyses
          <% end %>
          
          <%= link_to components_path, 
              class: "inline-flex items-center px-1 pt-1 border-b-2 #{controller_name == 'components' ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" do %>
            <i class="fas fa-microchip mr-1"></i>
            Composants
          <% end %>
          
          <%= link_to documentation_path, 
              class: "inline-flex items-center px-1 pt-1 border-b-2 #{current_page?(documentation_path) ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" do %>
            <i class="fas fa-book mr-1"></i>
            Documentation
          <% end %>
        </div>
      </div>
      <div class="hidden sm:ml-6 sm:flex sm:items-center">
        <%= link_to new_analysis_path, class: "ml-3 inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" do %>
          <i class="fas fa-plus mr-2"></i>
          Nouvelle analyse
        <% end %>
      </div>
    </div>
  </div>
</nav> 